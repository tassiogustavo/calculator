import 'package:flutter/material.dart';

class SetResultText extends StatelessWidget {
  const SetResultText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
